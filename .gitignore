# Python 快取檔案,不需要上傳
__pycache__/
*.pyc

# 注意:weights_meta.json、weights.npz 和 tokenizer.json 不要加進這裡!
# Vercel 上的伺服器是靠這三個檔案載入模型(不需要 torch、不需要 checkpoint.pt),
# 必須靠你在本機依序執行:
#   1. python train.py              (預訓練,產生 checkpoint.pt)
#   2. python prepare_sft_data.py   (把 data/*.jsonl 轉成結構化資料)
#   3. python train_sft.py          (問答微調,更新 checkpoint.pt)
#   4. python export_weights.py     (轉出 weights_meta.json + weights.npz)
# 然後把 weights_meta.json、weights.npz 和 tokenizer.json 一起 commit 上傳,
# 伺服器才有東西可以載入。
#
# checkpoint.pt 檔案很大(架構放大後常常超過 GitHub 單檔 100MB 上限),
# 只有在本機要繼續訓練(resume)時才需要,不commit它,加進這裡忽略掉。
checkpoint.pt

# 舊版匯出格式(純文字 JSON),架構放大後檔案會超過 GitHub 100MB 上限,
# 已經改用 weights_meta.json + weights.npz(見上面說明),不要再 commit 這個。
weights.json

# sft_data.jsonl 是 prepare_sft_data.py 自動產生的中間檔案,
# 隨時可以用 qa.txt / chat.txt 重新產生,不需要 commit。
sft_data.jsonl

# 第三方 AI API 金鑰,絕對不要 commit 上傳。
# 本機執行 server.py 時,靠 python-dotenv 讀取這個檔案;
# Vercel 上則要另外在 Vercel 專案設定裡加同樣名稱的環境變數
# (Settings → Environment Variables),不是靠這個檔案。
.env

# Groq 批次生成語料的執行 log,只是本機除錯用,不需要 commit。
_groq_big.log

