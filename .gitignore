# Python 快取檔案,不需要上傳
__pycache__/
*.pyc

# NEXUX v1.0 之後(見 docs/MODEL_MIGRATION.md),正式站改用微調過的
# 預訓練模型(weights_meta_pretrained.json + weights_pretrained.npz +
# vocab_pretrained.txt,那三個檔案還是要 commit,不要加進這裡)。
#
# 舊版(從零訓練的 char-level 模型)已經正式下架、不再部署,
# weights_meta.json / weights.npz / tokenizer.json 這三個檔案改成只封存在
# 本機和 git 歷史紀錄裡(舊 commit 仍可以找到、checkout 出來比較),
# 之後不再需要 commit 新版本,加進這裡忽略掉,避免不小心又推上去。
weights_meta.json
weights.npz
tokenizer.json

# checkpoint.pt 檔案很大(架構放大後常常超過 GitHub 單檔 100MB 上限),
# 只有在本機要繼續訓練(resume)舊版 char-level 模型時才需要,不commit它。
checkpoint.pt

# 預訓練模型微調(見 docs/MODEL_MIGRATION.md)本機用的中繼檔案,道理跟
# checkpoint.pt 一樣:很大(含 optimizer 狀態,超過 1GB)、只有本機繼續
# 微調時才需要,部署用的是 export_pretrained.py 匯出的
# weights_meta_pretrained.json + weights_pretrained.npz,不需要 commit 這個。
checkpoint_pretrained.pt

# verify_conversion.py 產生的本機驗證用中繼檔案(未量化的完整精度版本,
# 檔案很大),只是拿來跟 HuggingFace 原始模型比對數值用,不是部署格式。
pretrained_test/

# 舊版匯出格式(純文字 JSON),架構放大後檔案會超過 GitHub 100MB 上限,
# 已經改用 weights_meta.json + weights.npz(見上面說明),不要再 commit 這個。
weights.json

# sft_data.jsonl 是 prepare_sft_data.py 自動產生的中間檔案,
# 隨時可以用 qa.txt / chat.txt 重新產生,不需要 commit。
sft_data.jsonl

# 第三方 AI API 金鑰,絕對不要 commit 上傳。
# 本機執行 server.py 時,靠 python-dotenv 讀取這個檔案;
# Vercel 上則要另外在 Vercel 專案設定裡加同樣名稱的環境變數
# (Settings → Environment Variables),不是靠這個檔案。
.env

# Groq 批次生成語料的執行 log,只是本機除錯用,不需要 commit。
_groq_big.log

# PyInstaller 打包 local_python_agent.py 成 local_python_agent.exe 時
# 產生的中繼檔案,只有最終的 .exe 需要 commit(給網站訪客下載用),
# 這些建置過程的暫存目錄/spec檔不需要。
build_agent/
dist_agent/
*.spec

